이 프로젝트는 Unity 프로젝트이므로 Python의 `requirements.txt` 파일을 사용하지 않습니다.

프로젝트의 종속성(패키지)은 Unity 패키지 관리자(Unity Package Manager)를 통해 관리됩니다.

주요 종속성 목록은 `README.md` 파일에서 확인할 수 있으며, 전체 패키지 목록은 프로젝트의 루트 디렉토리에 있는 `Packages/manifest.json` 파일에 정의되어 있습니다.
